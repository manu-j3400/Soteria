# Ignore frontend modules
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.vercel/

# Ignore backend modules
backend/__pycache__/
backend/venv/
backend/.venv/
backend/*.pyc
__pycache__/
*.pyc

# Ignore environment variables
.env
.env*.local
frontend/.env
backend/.env

# OS / IDE junk
.DS_Store
*.log

# IDE / editor configs
.cursor/
.idea/

# Temporary generated files
tmp-antigravity-skills/

# Vercel CLI env file — contains OIDC tokens + Supabase keys, never commit
frontend/.env.vercel
.env.vercel

# Local databases — contain real user scan data, never commit
*.db
middleware/scan_history.db

# Automation task queue (runtime state)
automation_queue.json

# ML model backups (auto-created by retrain pipeline)
backend/ML_master/backups/

# Test artifacts
middleware/test_*.json
middleware/test_*.py
middleware/test_*.pdf

# Build artifacts
frontend/vite.config.d.ts
frontend/pnpm-lock.yaml
.vercel/
LOGO/

# GCN/SNN training artifacts (generated, not source-controlled)
backend/GCN_master/
backend/ML_master/acidModel_gcn.pt
engines/kyber/snn/snn_baseline.pt

# Scraped training data — generated by ingest_multi.py / scrape_cve.py
# May contain sensitive data from public code (hardcoded keys, tokens, etc.)
backend/data/external/huggingface_raw.csv

# Sandbox execution artifacts (from SNN bootstrap malware corpus)
malicious.dat
pickled_exploit
myfile.txt

# MCP server build artifacts
engines/ruflo/soteria-mcp/node_modules/
engines/ruflo/soteria-mcp/dist/
engines/ruflo/soteria-mcp/package-lock.json

# Claude Code local config
.claude/

# AI coding tool artifacts (Jules, Codex, Playwright MCP, agents)
.Jules/
.agents/
.codex/
.playwright-mcp/

# CVE scraper output — large generated files, may contain scraped code
backend/CSV_master/cve_features.csv
backend/CSV_master/.cve_scraper_state.json

# Temporary test screenshots
result_*.png
scanner_*.png
